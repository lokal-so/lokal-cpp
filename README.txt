# Lokal.so C++ library

Install:

  git clone https://github.com/lokal-so/lokal-cpp.git;
  cd lokal-cpp;
  sudo apt install -y make gcc g++ libcurl4-gnutls-dev;
  make;
  make example;
  ./example;

License:

  GPL-2.0-only

Maintainer:

  - Ammar Faizi <ammarfaizi2@gnuweeb.org>

-- 
Ammar Faizi
